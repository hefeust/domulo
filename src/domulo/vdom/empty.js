
import { BlockSorts } from '@/src/domulo/vdom/data-blocks'

export const empty = (bmp) => {
  // const emptyBlock = bmp.getEmptyBlock()
  
  // emptyBlock.sort = 'VTREE('
  
  return {
    uid: '0'
  }
}